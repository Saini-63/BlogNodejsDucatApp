{{>header}}
<div class="container mt-5">
    <div class="row">
        <div class="col-sm-6">
            <img src="{{blog.image}}" alt="{{blog.title}}" style="height: 500px; width: 100%">
        </div>
        <div class="col-sm-6">
            <h1>{{blog.title}}</h1>
            <p>{{blog.description}}</p>
        </div>
    </div>
</div>
{{>footer}}